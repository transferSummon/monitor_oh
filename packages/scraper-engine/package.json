{
  "name": "scraper-engine",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "scrape": "tsx src/cli.ts",
    "test": "npm run build && node --test $(find dist/tests -name '*.test.js' -print)"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "p-limit": "^6.2.0",
    "playwright": "^1.52.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.4",
    "typescript": "^5.7.2"
  }
}
